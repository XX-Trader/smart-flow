# ========================================
# 本地开发环境配置模板
# ========================================
# 使用方法:
#   1. 复制此文件为 .env.local
#   2. 修改配置值
#   3. 确保 .env.local 已添加到 .gitignore
# ========================================

# ========================================
# Django 配置
# ========================================

# Django 密钥（生成方式：python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"）
DJANGO_SECRET_KEY=django-insecure-change-this-in-production

# 调试模式（本地开发设为 True）
DEBUG=True

# 允许的主机（本地开发）
ALLOWED_HOSTS=localhost,127.0.0.1

# ========================================
# 数据库配置 (MySQL)
# ========================================

# 数据库引擎
DB_ENGINE=django.db.backends.mysql

# 数据库主机
DB_HOST=127.0.0.1

# 数据库端口
DB_PORT=3306

# 数据库名称
DB_NAME=your_database

# 数据库用户
DB_USER=your_user

# 数据库密码
DB_PASSWORD=your_password

# ========================================
# Redis 配置
# ========================================

# Redis 主机
REDIS_HOST=127.0.0.1

# Redis 端口
REDIS_PORT=6379

# Redis 数据库（0-15）
REDIS_DB=0

# Redis 密码（如果设置了密码）
# REDIS_PASSWORD=your-redis-password

# ========================================
# API 加密配置
# ========================================

# API Secret 加密主密钥（生成方式：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"）
# 警告：此密钥用于加密 API Secret，丢失后所有 API Secret 将无法解密！
API_SECRET_MASTER_KEY=your-api-secret-master-key-here

# ========================================
# Celery 配置
# ========================================

# Celery Broker (Redis)
CELERY_BROKER_URL=redis://127.0.0.1:6379/0

# Celery Result Backend
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0

# ========================================
# CORS 配置（开发环境）
# ========================================

# 允许的前端域名（逗号分隔）
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:8081,http://localhost:8082,http://localhost:8083

# ========================================
# 邮件配置（可选）
# ========================================

# 邮件后端
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP 服务器
# EMAIL_HOST=smtp.gmail.com

# SMTP 端口
# EMAIL_PORT=587

# 发件邮箱
# EMAIL_HOST_USER=your-email@gmail.com

# 邮箱密码
# EMAIL_HOST_PASSWORD=your-email-password

# 使用 TLS
# EMAIL_USE_TLS=True

# ========================================
# 第三方服务（可选）
# ========================================

# Sentry 错误追踪
# SENTRY_DSN=your-sentry-dsn

# GitHub OAuth（如果使用 GitHub 登录）
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
