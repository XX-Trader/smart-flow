# 文档同步检查技能 - 实施文档

> **版本**: v1.0.0
> **创建日期**: 2026-01-21
> **作者**: Smart Flow Team
> **状态**: 设计中

---

## 📋 需求概述

### 功能目标

创建一个**文档同步检查技能**，在代码修改后自动检查文档是否需要同步更新。

### 核心场景

1. **API 接口变更**：新增/删除/修改 API 接口时，检查 API 文档是否同步
2. **数据库字段变更**：新增/删除/修改数据库字段时，检查数据库文档是否同步
3. **需求讨论时**：主动询问是否需要编写/更新文档

### 设计原则

- ✅ **语言无关**：支持任何编程语言（Python、JavaScript、Go、Java 等）
- ✅ **自动化触发**：代码修改后自动检查，无需手动调用
- ✅ **极简输出**：只报告需要关注的同步问题
- ✅ **非侵入式**：不强制要求文档格式，只做提醒

---

## 🏗️ 技术方案

### 方案架构

```
┌─────────────────────────────────────────────────────────────┐
│                     代码修改完成                              │
│                   (PostTask Hook 触发)                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              doc-sync-checker 技能执行                       │
│                                                             │
│  1. 分析变更文件类型                                         │
│  2. 提取 API 接口定义（路由/控制器）                          │
│  3. 提取数据库模型定义（Model/Schema）                       │
│  4. 扫描 docs/ 文件夹相关文档                                │
│  5. 对比并生成同步报告                                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    同步报告输出                              │
│                                                             │
│  ✅ API 文档已同步                                          │
│  ⚠️  API 文档需要更新                                       │
│  ⚠️  数据库文档需要更新                                     │
│  ❓ 未找到相关文档，是否需要创建？                           │
└─────────────────────────────────────────────────────────────┘
```

### 触发机制

| 方案 | 实现方式 | 优点 | 缺点 | 选择 |
|------|----------|------|------|------|
| **PostTask Hook** | 在任务完成后自动触发 | 完全自动化 | 需要配置 Hook | ✅ 采用 |
| **关键词触发** | 用户说"检查文档"时触发 | 简单直接 | 需要手动调用 | ✅ 辅助 |
| **Git Hook** | Git commit 前触发 | 拦截提交 | 需要配置 Git | ❌ 暂不采用 |

---

## 📁 文件结构

```
smart-flow/
├── skills-user/
│   └── doc-sync-checker/              # 新建技能目录
│       ├── SKILL.md                    # 技能主文件
│       ├── config/                     # 配置文件
│       │   ├── patterns.json           # 代码模式定义
│       │   └── keywords.json           # 关键词配置
│       ├── analyzers/                  # 代码分析器
│       │   ├── base-analyzer.md        # 基础分析器
│       │   ├── python-analyzer.md      # Python 分析器
│       │   ├── javascript-analyzer.md  # JavaScript 分析器
│       │   ├── go-analyzer.md          # Go 分析器
│       │   └── java-analyzer.md        # Java 分析器
│       └── templates/                  # 文档模板
│           ├── api-doc-template.md     # API 文档模板
│           └── db-doc-template.md      # 数据库文档模板
└── .claude/
    └── hooks/
        └── post-task.md                # 更新：添加文档检查逻辑
```

---

## 🔍 代码分析逻辑

### API 接口检测

| 语言/框架 | 检测模式 | 示例 |
|-----------|----------|------|
| **Python/Django** | `@api_view`, `path()`, `urlpatterns` | `path('api/users/', views.user_list)` |
| **Python/FastAPI** | `@app.get`, `@app.post`, `router` | `@app.get("/api/users")` |
| **JavaScript/Express** | `app.get`, `app.post`, `router` | `app.get('/api/users')` |
| **Go/Gin** | `router.GET`, `router.POST` | `router.GET("/api/users")` |
| **Java/Spring** | `@GetMapping`, `@PostMapping` | `@GetMapping("/api/users")` |

### 数据库模型检测

| 语言/框架 | 检测模式 | 示例 |
|-----------|----------|------|
| **Python/Django** | `class Model(models.Model)` | `class User(models.Model)` |
| **Python/SQLAlchemy** | `class Model(Base)` | `class User(Base)` |
| **JavaScript/Prisma** | `model Model` | `model User { }` |
| **JavaScript/Mongoose** | `new Schema()` | `new Schema({ name: String })` |
| **Go/GORM** | `type Model struct` | `type User struct` |
| **Java/JPA** | `@Entity` | `@Entity public class User` |

---

## 📊 报告格式

### 同步状态报告

```markdown
## 📄 文档同步检查报告

### 检查时间
2026-01-21 15:30:00

### 变更文件
- [frontend/src/api/user.ts](frontend/src/api/user.ts)
- [backend/views/user_views.py](backend/views/user_views.py)
- [backend/models/user.py](backend/models/user.py)

### API 接口变更
| 接口 | 变更类型 | 文档状态 |
|------|----------|----------|
| GET /api/users/ | 新增 | ⚠️ 需要更新 |
| POST /api/users/create | 修改 | ⚠️ 需要更新 |

### 数据库字段变更
| 表名 | 字段 | 变更类型 | 文档状态 |
|------|------|----------|----------|
| users | phone | 新增 | ⚠️ 需要更新 |

### 建议操作
- [ ] 更新 [docs/api/user-api.md](docs/api/user-api.md)
- [ ] 更新 [docs/database/user-schema.md](docs/database/user-schema.md)

### 快速操作
是否需要我帮你生成文档模板？
- 输入 "生成 API 文档" 自动生成 API 文档
- 输入 "生成数据库文档" 自动生成数据库文档
```

---

## 🎯 执行流程

### 自动流程（PostTask Hook）

```
代码修改完成
    ↓
PostTask Hook 触发
    ↓
判断是否涉及接口/数据库文件
    ↓
是 → 调用 doc-sync-checker 技能
    ↓
分析代码变更
    ↓
扫描 docs/ 文件夹
    ↓
生成同步报告
    ↓
输出报告并询问是否需要帮助
```

### 手动调用

用户可以说：
- "检查文档同步"
- "文档是否需要更新"
- "docs-sync-check"

---

## 📝 配置文件

### `config/patterns.json`

```json
{
  "version": "1.0.0",
  "api_patterns": {
    "python": {
      "django": [
        "path\\(['\"]([^'\"]+)['\"]",
        "@api_view",
        "router.register"
      ],
      "fastapi": [
        "@app\\.(get|post|put|delete)\\(['\"]([^'\"]+)['\"]",
        "router\\.(get|post|put|delete)"
      ]
    },
    "javascript": {
      "express": [
        "app\\.(get|post|put|delete)\\(['\"]([^'\"]+)['\"]",
        "router\\.(get|post|put|delete)"
      ]
    }
  },
  "db_patterns": {
    "python": {
      "django": ["class \\w+\\(models\\.Model\\)"],
      "sqlalchemy": ["class \\w+\\(Base\\)"]
    },
    "javascript": {
      "prisma": ["model \\w+\\s*\\{"],
      "mongoose": ["new Schema\\("]
    }
  }
}
```

### `config/keywords.json`

```json
{
  "version": "1.0.0",
  "trigger_keywords": [
    "检查文档",
    "文档同步",
    "更新文档",
    "docs-sync"
  ],
  "api_keywords": [
    "接口",
    "api",
    "endpoint",
    "路由",
    "router"
  ],
  "db_keywords": [
    "数据库",
    "model",
    "schema",
    "字段",
    "表"
  ]
}
```

---

## 🚀 实施步骤

### 第一阶段：核心功能（MVP）

1. ✅ 创建技能基础结构
2. ✅ 实现基础分析器（检测文件类型）
3. ✅ 实现 Python/Django 分析器
4. ✅ 实现 API 接口检测
5. ✅ 实现数据库模型检测
6. ✅ 实现文档扫描逻辑
7. ✅ 实现报告生成

### 第二阶段：扩展支持

1. ⏳ 支持 FastAPI、Flask
2. ⏳ 支持 JavaScript/Express
3. ⏳ 支持 Go/Gin
4. ⏳ 支持更多文档格式

### 第三阶段：智能优化

1. ⏳ 自动生成文档模板
2. ⏳ Git diff 智能对比
3. ⏳ 学习项目文档结构

---

## 📚 参考资料

- [PostTask Hook 文档](.claude/hooks/post-task.md)
- [feature-development 技能](skills-user/feature-development/SKILL.md)
- [Django URL 调度器](https://docs.djangoproject.com/en/stable/topics/http/urls/)
- [FastAPI 路由操作](https://fastapi.tiangolo.com/tutorial/path-operations/)

---

## 🔄 更新日志

### v1.0.0 (2026-01-21)
- 🎉 初始版本设计
- 📝 完成技术方案设计
- 📋 制定实施计划
