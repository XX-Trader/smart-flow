# ⚡ Smart Flow 本地使用 - 2分钟快速上手

## 📦 方法 1: Windows PowerShell（推荐）

打开 **PowerShell**，复制粘贴以下命令：

```powershell
# 1. 创建目录
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude\skills"

# 2. 复制插件
Copy-Item -Path "d:\学习资料\量化交易\大白文件2021.5.31\CoinGod_Web\smart-flow" -Destination "$env:USERPROFILE\.claude\skills\" -Recurse -Force

# 3. 验证安装
Write-Host "✅ Smart Flow 已安装！" -ForegroundColor Green
Write-Host "位置: $env:USERPROFILE\.claude\skills\smart-flow" -ForegroundColor Cyan
Get-ChildItem "$env:USERPROFILE\.claude\skills\smart-flow\skills"
```

## 📦 方法 2: Git Bash（推荐）

打开 **Git Bash**，复制粘贴以下命令：

```bash
# 1. 创建目录
mkdir -p ~/.claude/skills

# 2. 复制插件
cp -r "/d/学习资料/量化交易/大白文件2021.5.31/CoinGod_Web/smart-flow" ~/.claude/skills/

# 3. 验证安装
echo "✅ Smart Flow 已安装！"
echo "位置: ~/.claude/skills/smart-flow"
ls ~/.claude/skills/smart-flow/skills
```

## ⚙️ 配置 Claude Code

### Windows 用户

1. 按 `Win + R`，输入 `notepad`
2. 打开文件：`C:\Users\你的用户名\.claude\CLAUDE.md`
3. 在文件末尾添加（复制以下内容）：

```markdown
## Smart Flow

You have Smart Flow installed. Smart Flow is an intelligent task orchestration plugin.

**When to use:**
- User wants to build a feature
- Requirements are unclear or incomplete

**How to use:**
- `/smart-flow` or `/sf` (manual trigger)
- Auto-trigger when requirements vague

**Workflow:**
1. Requirements Clarity - Ask questions
2. Task Decomposer - Break down tasks
3. Parallel Executor - Execute concurrently
4. Result Synthesizer - Generate report
```

4. 保存文件（Ctrl+S）

## 🚀 立即测试

启动 **Claude Code**：

```bash
claude
```

在 Claude Code 中输入：

```
/smart-flow
```

或测试实际功能：

```
/smart-flow
我想做一个简单的待办事项应用
```

## ✅ 验证成功

成功的标志：

1. ✅ `C:\Users\你的用户名\.claude\skills\smart-flow\` 目录存在
2. ✅ 其下有 `skills\` 目录，包含 5 个技能
3. ✅ `C:\Users\你的用户名\.claude\CLAUDE.md` 包含 Smart Flow 配置
4. ✅ Claude Code 中 `/smart-flow` 能触发

## 🎯 第一次使用

在 Claude Code 中试试：

```
/smart-flow
帮我做一个用户登录功能，支持邮箱和手机号登录
```

Smart Flow 会：
1. 问你一些问题（需求澄清）
2. 自动分解任务
3. 并行执行多个专家 agent
4. 生成完整报告

## 📚 更多文档

- [QUICK_START.md](QUICK_START.md) - 快速开始
- [docs/LOCAL_USAGE_GUIDE.md](docs/LOCAL_USAGE_GUIDE.md) - 详细使用指南
- [README.md](README.md) - 项目介绍

## 🐛 遇到问题？

### 问题：找不到 .claude 目录

**解决**：
```powershell
# 创建目录
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude"
```

### 问题：/smart-flow 无反应

**解决**：
1. 确认 CLAUDE.md 已正确配置
2. 重启 Claude Code
3. 检查技能文件是否存在

### 问题：复制失败

**解决**：
- 确保使用 PowerShell 或 Git Bash
- 检查源路径是否正确
- 确保目标目录有写权限

---

**🎉 安装完成！现在可以使用 Smart Flow 了！**
